import React from 'react';

const BlogCard = ({
  title,
  description,
  date,
  author,
  image,
  link,
}: {
  title: string;
  description: string;
  date: string;
  author: string;
  image: string;
  link: string;
}) => {
  return <div>BlogCard</div>;
};

export default BlogCard;
